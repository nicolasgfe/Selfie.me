import { Box, Text } from "@chakra-ui/react"

const theme = extendTheme({
    components: {
      Button,
    },
  })

export const Home = () => {
    return (
                
           

        
    )
}