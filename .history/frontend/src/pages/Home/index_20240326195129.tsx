import { Box, Text, extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
    components: {
      Foo,
    },
  })

export const Home = () => {
    return (
                
           

        
    )
}